# HA Somfy — Pending Items

Living backlog for the HA Somfy integration. **Continuously revised**: items are added as they
surface, marked complete when verified, and deleted when no longer relevant.

- **Status:** `open` · `in-progress` · `blocked` · `parked` · `done` (move to [Completed](#completed))
- **Priority:** `H` high · `M` medium · `L` low
- **IDs are stable** — never renumber or reuse an ID, even after deletion.

> ## Standing constraint — WORK-01
>
> **DO NOT DELETE the 24 existing `cover.*` entities.** They are **not** orphans: they are live
> entities bridged into HA via **Homebridge**, merely `unavailable` at the time of inspection. An
> earlier reading of this backlog wrongly identified them as leftovers from a failed integration
> and recommended purging them; deleting them would have destroyed working configuration.
> `restored: true` plus a 1:1 name match to the 24 groups is **not** evidence an entity is dead —
> here it meant the same groups were already reaching HA by another route.
>
> This is a rule, not a task. It has no completion state and must never be moved to Completed.

---

## 1. Entity migration — the active workstream

The integration works; what remains is handing the existing Home Assistant configuration over to
it. **The approach changed on 2026-07-30**: instead of HA Somfy keeping its own IDs and every
reference being rewritten, HA Somfy *adopts* the Homebridge entity IDs and Homebridge moves aside
to `*_2`. Every reference is by entity ID, so they then resolve to the new integration untouched.

Ordered table and the two ways this can silently go wrong:
[`entity-migration-map.md`](entity-migration-map.md).

| ID | Item | Pri | Status | Done when |
|----|------|-----|--------|-----------|
| MIGRATE-09 | **Rewrite what entity-ID inheritance did not cover** — references by `device_id` (device triggers/actions, some cards) still pointing at Homebridge devices, and the `cover.set_cover_position` calls against the four Irismo groups, which were no-ops via Homebridge and must become open/close. | H | open | No `device_id` references to Homebridge covers remain, and no `set_cover_position` against the four Irismo groups |
| NAME-02 | **Display names now come from the gateway, not Homebridge.** `cover.roomi_drapes` reads "RoomI SH" rather than "RoomI Drapes". Cosmetic, and invisible on cards that set their own `name:`. Options: per-entity name overrides in HA, rename the groups in the UAI+ web UI so both agree, or accept it. | L | open | Owner decides; no action is a valid outcome |

Nothing else is outstanding. The migration above is the only open work.

## Completed

| ID | Item | Done | Evidence |
|----|------|------|----------|
| PROTO-01 | Built `scripts/probe_uai.py`, query-only with an enforced allowlist | 2026-07-29 | Ran clean against all 49 nodes; `tests/test_probe_safety.py` proves 13 mutating/unknown methods are refused and 4 read-only ones permitted |
| PROTO-02 | **Irismo reports `type: "SDN Module"`** — all 9, identically. The string `irismo` never appears; the gateway names the 1811129 bridge, not the motor. All 9 refuse position with `{"error":-32600}` | 2026-07-29 | `tests/fixtures/bus_inventory.json` — histogram `Sonesse 50DC=28, Sonesse 30=12, SDN Module=9` |
| PROTO-03 | `params` is a **list of single-key dicts**, not a bare dict | 2026-07-29 | Runtime shape detection returned `list`; 149 successful requests |
| PROTO-04 | Group address = `"0101" + f"{index:02X}"`, index 1-based from `somfy_groups.json`. Group 1 `ALL` is a broadcast with no stored membership | 2026-07-29 | All 24 member-bearing groups matched by name, e.g. `010106`→`RoomG B/O`, `010119`→`RoomK SH` |
| PROTO-05 | Bus is fast and reliable: 149 requests in 5.0 s, mean 0.03 s, **zero retries**. One transient single-node dropout observed → HW-02 | 2026-07-29 | `timings` in probe output; 6 discovery passes |
| PROTO-06 | Sanitised fixtures committed; raw captures gitignored | 2026-07-29 | `tests/fixtures/*.json` committed; credential scan across all committable files came back clean |
| PROTO-08 | **Position polarity confirmed correct.** `GATEWAY_POSITION_IS_INVERTED = True` stands; Somfy 0 = open, HA 0 = closed. Settled with **zero movement**. | 2026-07-29 | Live entities read closed for all four bathroom/toilet shades and both guest bed blackouts, open for everything else; owner confirmed that matches the physical blinds |
| PROTO-09 | **The `sdn.` prefix is optional.** `status.info`, `status.position` and `group.get` return payloads identical to their prefixed forms. Usable as a fallback; no reason to switch. | 2026-07-29 | `scripts/probe_open_questions.py`, all three pairs identical |
| PROTO-07 | **Answered — the endpoints do unlock, but are not worth adopting.** Owner spotted that `pilot.htm` has its own password. Auth is `GET /password.cgi?VERIFY=<web password>`, a separate credential from telnet, and it sets **no cookie** — the session is IP-bound. Afterwards `somfy_devices.json` returns all 49 labels in **one** request, including the two nodes the per-node endpoint never served, and `somfy_presets.json` returns 16 slots per node. **All 16 are empty on all 49 nodes**, so there is no preset feature to build. Not adopted: discovery already names all 49 correctly without it, and adopting would add a second credential to the config flow for every user plus IP-bound session handling. | 2026-07-30 | `scripts/probe_web_session.py`; 49 devices, 49 labels, 0 presets |
| CAP-01 | Classifier maps type string → capability, probing unknown types | 2026-07-29 | `tests/test_models.py`; replaying the real 49-node inventory splits exactly 40 positional / 9 non-positional with 0 unknown |
| CAP-03 | **`supported_features` is never hardcoded — the fix this project exists for, verified in CI and on real hardware.** Four all-Irismo groups report `supported_features: 11` with `assumed_state: true`; all twenty Sonesse groups report `15` with real positions. | 2026-07-29 | CI: `test_irismo_does_not_advertise_set_position` and `test_sonesse_does_advertise_set_position`. Live: `batch_get_state` across all 24 group entities |
| CAP-04 | `bool` rejected before any numeric position check | 2026-07-29 | `test_bool_is_never_a_position`; the probe's own version of this bug was found and fixed |
| CAP-05 | Classifier matches `SDN Module`, not `irismo` | 2026-07-29 | `test_sdn_module_is_non_positional`; `irismo` retained only as a defensive alias |
| CAP-06 | A `false` reading leaves capability untouched | 2026-07-29 | `test_the_false_position_node_keeps_its_capability` against real node `07753E` |
| GROUP-01 | Group cover entities, open/close/stop | 2026-07-29 | CI: `test_all_irismo_group_has_no_position_slider`, `test_all_sonesse_group_has_a_position_slider` |
| GROUP-02 | Per-motor group membership exposed as a static entity attribute | 2026-07-30 | `cover.py:206-211` returns `node_id`, `motor_type`, `capability` and `groups`, with an explicit comment that only static values belong there. Untested — see TEST-01 |
| GROUP-03 | Group 1 `ALL` omitted from discovery | 2026-07-29 | `UaiClient.async_get_groups` filters it; `test_discovery_skips_the_all_group` |
| GROUP-04 | Groups inherit least-capable member | 2026-07-29 | CI: `test_mixed_group_falls_back_to_least_capable` |
| PERF-01 | Change-gated state writes | 2026-07-29 | CI: `test_state_is_written_only_when_something_changed` — 3 refreshes, 1 write |
| PERF-02 | Only position-capable motors are polled | 2026-07-29 | `SomfyCoordinator._async_update_data` skips non-positional nodes; the 9 Irismo generate no poll traffic |
| PERF-03 | **A moving cover updates promptly instead of waiting up to 60 s.** `async_follow_movement` polls only the nodes just commanded, every 2 s, dropping each after 3 unchanged reads (~6 s, covering motor start-up), with a 120 s ceiling. Non-positional nodes are filtered out entirely, so the nine Irismo add no traffic; entities stay change-gated, so a no-op poll writes nothing. | 2026-07-29 | 7 coordinator tests in CI, plus owner confirmation on live hardware: "the lag is gone, it updates nicely" |
| PERF-04 | `iot_class` declared `local_polling` | 2026-07-29 | `manifest.json`; zero unsolicited notifications were seen in 149 probe requests |
| HA-01 | Config flow authenticates for real | 2026-07-29 | CI: `test_bad_credentials_are_rejected` |
| HA-02 | Config flow uses auto-discovery | 2026-07-29 | CI: `test_confirm_step_reports_the_capability_split`; no hand-typed motors anywhere |
| HA-03 | Single-argument step handlers | 2026-07-29 | CI: `test_flow_completes_and_creates_an_entry` — the flow reaches CREATE_ENTRY |
| HA-04 | `diagnostics.py` with credentials redacted | 2026-07-29 | `async_get_config_entry_diagnostics` redacts username and password via `TO_REDACT` |
| HA-05 | One device per motor | 2026-07-29 | CI: `test_each_motor_is_its_own_device_showing_its_model` |
| HA-06 | **`via_device` referenced a device that did not exist.** Motor devices declare the gateway as parent, but the gateway was only created implicitly by the group entities, and motors were added first. HA warned and said it would stop working in a later release. Fixed by registering the gateway explicitly during setup, before any entity is added, and by adding groups ahead of motors. | 2026-07-29 | Found in `get_error_log` during live validation; fixed in v0.1.3. **Confirmed gone after installing v0.1.3 and rebooting: 0 occurrences of `ha_somfy`, `via_device` or `non existing` across the full 1003-line log.** The log covers a fresh boot and still carries `rinnai`'s setup-time `helpers/frame` warning 12 times — the same warning type that previously shared a line with ours — so the absence is real, not an empty buffer |
| HW-02 | Transient node dropout tolerated | 2026-07-29 | `SomfyCoordinator.async_discover` requires 3 consecutive misses before dropping a node |
| HW-03 | **Void — there is no duplicate name.** `136E33` is `RoomA SH 2`, not `RoomA B/O 2`; confirmed by the owner's web UI, the gateway's HTTP `LABEL`, and a later telnet read. The 14:04 capture recorded the wrong name while that node's group membership (`010103` = `RoomA SH`) already said otherwise — the contradiction was in the data and went unchecked. Nothing to rename. The uniqueness guard stays as a defensive measure. | 2026-07-29 | §10 of the protocol findings; superseded by the HW-04 guard |
| HW-04 | **Discovery warns when a node's name points at a group it is not in.** Narrow by design: whitespace and case are ignored, so `RoomI SH 1` in `RoomI SH` is silent, and a name resembling no other group, like `RoomD Hall SH` in `RoomD SH`, is silent too. Only a name matching *another existing group* warns — which is exactly the `136E33` case. | 2026-07-29 | `find_name_group_conflicts` + 7 tests, including one asserting the current 49-node fleet produces **zero** warnings |
| NAME-01 | **Names resolved from two sources.** `sdn.status.info` answers a *first* read with a name synthesised from group membership when the gateway has not yet read the device's own label; the query triggers that read and a second call returns the truth. Discovery therefore reads each node twice and prefers the gateway's HTTP `LABEL`. Matters because entity IDs derive from the name and are assigned once. | 2026-07-29 | Verified live: 49 nodes, 0 unnamed, 46 from HTTP and 3 from the second telnet read — including `40FD89`, which HTTP never served |
| OPEN-01 | **New entities land with their own IDs; references get migrated.** No renaming of the Homebridge entities is needed — a collision check confirmed none of the 24 predicted new IDs already exists, so Home Assistant will not append `_2`. | 2026-07-29 | Mapping documented in [`entity-migration-map.md`](entity-migration-map.md) |
| MIGRATE-01 | Entity IDs re-verified against the live registry | 2026-07-29 | All 24 observed as `cover.somfy_uai_*` — HA prefixes the gateway device name, so the predicted `cover.roomb_sh` form was wrong. Map corrected; member counts match the Phase 1 probe exactly |
| LIVE-01 | **Movement verified on real hardware — both paths.** Sonesse: `RoomM SH` (group `010110`, two motors) closed from 100 → 96, stopped mid-travel at 52, reopened to 100; owner confirmed both motors moved together. Irismo: `RoomD SH` (group `010112`, `40FCFC` behind the 1811129 bridge) closed and reopened on command, verified by eye since it reports no position by design. | 2026-07-29 | Live service calls plus owner observation |
| HACS-01 | HACS-native layout | 2026-07-29 | CI: HACS action reports **all 9 checks passed** (needed repo topics + brand assets) |
| HACS-02 | CI: hassfest + HACS + ruff + pytest | 2026-07-29 | All four jobs green on `main`; **140 tests passed** as of `61c36fa` |
| HACS-03 | Tagged semver releases so HACS offers updates | 2026-07-29 | Six releases `v0.1.0`–`v0.1.5`; v0.1.5 installed and running on the live system |
| HACS-04 | `requirements: []` maintained | 2026-07-29 | CI: hassfest passes with the client vendored and no external requirements |
| IRIS-01 | **Open/closed state for Irismo motors.** Telnet refuses `sdn.status.position` for all nine SDN Module nodes, and this project read that as "no state exists". Too strong: the gateway holds their open/closed state and serves it from `somfy_device.json`. Verified on hardware — opening `RoomD SH` flipped `40FCFC` from `1000 (100 %)` to `0 (0 %)` in **under two seconds**, quicker than the drape can travel, while two control nodes held at `1000`; closing restored it. So it is modelled from the last command, not measured: correct for open/closed, never intermediate, and genuinely assumed. **Capability is untouched** — an Irismo stays non-positional and gains no slider. Needs the web password, which is optional and off by default. | 2026-07-30 | 232 tests in CI, up from 179: `test_web.py` (wrong-node guard, retry, 403 re-login, unencoded password), `test_irismo_state.py` (telnet still never asked, last-known state kept on a miss, no behaviour change without a password), `test_cover.py`, `test_diagnostics.py` |
| IRIS-02 | **Verified live: the four all-Irismo groups report open/closed.** `RoomI SH`, `RoomI B/O` and `RoomG SH` closed; `RoomD SH` open — all with `supported_features: 11` and `assumed_state: true`, so state was gained and no slider appeared. | 2026-07-30 | `batch_get_state` after installing v0.3.1. Stronger than the scripted check: `RoomD SH` read `open` because **the owner had opened it**, a change nothing in this session made or knew about, and the entity reflected it correctly |
| IRIS-04 | **The reason v0.3.0 did nothing, and why that took an hour to find.** A stale session does not answer 403 — it answers HTTP 200 carrying a *different node's* payload, indistinguishable from the buffer lag this endpoint also has. The `NODE` guard rejected those correctly, but re-authentication was wired only to 403, so nothing ever looked like an auth failure and the client retried a dead session forever. Now a read still rejected after two attempts clears the session, and a failed read does not hand its session to the next node. **Separately, the whole feature failed silently**: every path logged at debug and the owner's on-disk logging is off, so a wrong password was indistinguishable from an unreachable endpoint. Repeated failures now log one WARNING naming the last login result and the last read failure, and diagnostics gained a `web` section with the same counters. | 2026-07-30 | Released as v0.3.1 and confirmed working on live hardware; `test_persistent_wrong_node_answers_trigger_a_re_login` reproduces the deadlock |
| IRIS-05 | **Setup no longer blocks Home Assistant.** v0.3.0 read all 49 labels over HTTP at setup — one node per request from a slow embedded server, each retried up to five times — making `ha_somfy` the last integration to finish. Removed: the double telnet read already names every node correctly, and did so throughout the period when this HTTP path was failing silently and completely, which is the strongest possible evidence nothing depended on it. The group-name request is now retried three times and warns if it fails, rather than silently renaming all 25 groups. | 2026-07-30 | HA's own bootstrap warning — "Waiting for integrations to complete setup: ha_somfy" ×4, entities appearing ~5 min after start — was the evidence; released as v0.3.2 |
| IRIS-06 | **Group names restored, and the startup fix confirmed on hardware.** All 25 names are back — `Somfy UAI+ RoomD SH` rather than `Somfy UAI+ Group 18` — and the Irismo state feature survived the removal of the label sweep. | 2026-07-30 | After installing v0.3.2: friendly names correct across the fleet; the four all-Irismo groups still report open/closed at `supported_features: 11` with `assumed_state`, Sonesse groups still `15`. **The boot log carries no `ha_somfy` line at all** — no "waiting for integrations", no web-client warning, no group-names warning — and entities appeared ~30 s after start against ~5 min on v0.3.1 |
| AREA-01 | **All 24 group devices assigned to their rooms.** Three deliberately differ from where Homebridge had them, confirmed with the owner: both RoomJ groups sat in *RoomN* despite their names, and `RoomD Hallway Drapes` sat in *1F Hallway* though the drape is in the roomd living room. Those are corrections of long-standing Homebridge mis-assignments rather than migration artifacts — recorded in the map so nobody later "fixes" them back. | 2026-07-30 | Verified live: **0 of 24 devices without an area**, 0 entities without an area, 21 matching Homebridge exactly and 3 differing by intent |
| MIGRATE-07 | **The 24 Homebridge entities moved aside to `*_2`**, freeing their IDs without disturbing the references pointing at them. | 2026-07-30 | Verified live: all 24 `*_2` twins exist and still resolve |
| MIGRATE-08 | **HA Somfy adopted all 24 former Homebridge entity IDs**, so every existing dashboard, automation, script and scene reference now resolves to this integration with no rewriting. | 2026-07-30 | Verified live by comparing `integration_entities('ha_somfy')` against the expected 24: **0 not taken over, 0 unexpected, 0 bad states**. The four Irismo groups still report `supported_features: 11` with `assumed_state`; Sonesse groups still `15` with live positions |
| MIGRATE-06 | **One device per group.** All 24 groups hung off the single gateway device, which forced every group entity into whatever area the gateway was in. Each group is now its own device with the gateway as `via_device`. `unique_id` deliberately unchanged, so existing entities re-parent rather than being recreated — entity IDs, history and references all survive. | 2026-07-30 | Released as v0.4.0; 242 tests, including `test_a_group_keeps_its_unique_id`, which guards the property that would otherwise silently orphan all 24 entities |
| WORK-06 | Stale `ajguerre1/brands` fork deleted (created for the rejected PR #10871) | 2026-07-29 | `gh api repos/ajguerre1/brands` → **404**; `gh repo list ajguerre1 --fork` returns nothing; `ha-somfy` confirmed intact and not a fork |
| CAP-02 | **Per-motor capability override — the manual escape hatch when detection is wrong.** Applied at the *coordinator*, not the entity, because capability decides whether a node is polled at all and whether it is followed while moving; an entity-level override would produce a slider on a motor nothing reads. Reapplied after every discovery, since each pass rebuilds capability from the gateway's type string. The options flow became a menu — polling, or motor capability — and now **merges** rather than replaces, since `async_create_entry` overwrites the options dict wholesale and the naive version would have silently reset the poll interval the first time anyone set an override. | 2026-07-30 | 22 tests in CI across `test_capability_override.py` (polling and fast-follow consequences, reapplication, garbage input), `test_options_flow.py` (both merge directions) and `test_models.py` |
| TEST-01 | **Entity attributes are now covered.** The load-bearing assertion is not what the attributes contain but that they hold still: a blind travelling 0 → 100 writes state repeatedly, and an attribute tracking position would put a changed payload on every one of ~48 panels each time. Also covers the empty-dict path for a node or group that has left the bus. | 2026-07-30 | 6 tests in `tests/ha/test_cover.py`. Characterisation tests, not TDD — the behaviour already existed and was correct, which is precisely why the item was raised |
| WORK-03 | **`somfy-sdn` skill authored** at `.claude/skills/somfy-sdn/SKILL.md`. Organised around the five traps, because each cost real debugging here and none is visible from the wire: `params` is a list of single-key dicts; Irismo reports `SDN Module` and the string `irismo` never appears; `bool` subclasses `int` so `{"result": false}` reads as position 0; `false` means faulty hardware, not "no position"; and a first `status.info` can return a group-derived placeholder that becomes a permanently wrong entity ID. | 2026-07-30 | Committed. **Not subagent-tested** — the skill-authoring discipline calls for baseline scenarios against a fresh agent, which this session is not permitted to spawn |
| WORK-04 | **Resolved toward the honest description rather than the hook.** `CLAUDE.md` now states that pushing is a manual step. An unconditional `post-commit` auto-push cannot make the judgment the very next sentence demands — pause when the diff carries secrets, a force-push, or rewritten history — and would push a credential-bearing commit before anyone could intervene. `.git/hooks/` is untracked besides, so a hook would silently not exist in a fresh clone. | 2026-07-30 | `CLAUDE.md` § Git sync |
| HW-01 | **Closed — the finding is banked and nothing is outstanding here.** Two motors reply `false` to position and both are physically faulty: `07753E` "RoomB SH 2" and `077537` "RoomB B/O 3". All 38 healthy positional motors return a number, so the correlation is 2 for 2 — **`false` signals a motor-side fault, not a configuration gap.** That is why capability comes from the type string: a faulty Sonesse is never silently reclassified as an Irismo. **Self-healing on repair**, verified: both are already classified `POSITIONAL` from their type string, and `_async_update_data` polls every positional node each cycle, so the moment a repaired motor returns a number its position appears — no reload, no release, no code change. | 2026-07-30 | Software behaviour covered by CAP-06; recovery path confirmed in `coordinator.py` — capability is set from the type string at discovery and never demoted by a `false` reading |
| MIGRATE-05 | **Closed — `077537` "RoomB B/O 3" is out of service by the owner's choice and correctly modelled.** It was physically disconnected pending repair and removed from `RoomB B/O`, so that group truthfully has 2 members and `cover.somfy_uai_roomb_b_o` drives 2 of 3. It exists only as a disabled-by-default motor entity, generating no traffic. **One caveat on recovery, and the reason this row is worth reading after closure: membership is *not* picked up automatically.** `async_discover` runs once, at setup (`__init__.py:169`); the 60 s cycle refreshes positions of known nodes and never re-reads group membership. So after re-adding the motor in the UAI+ web UI, **reload the integration once**. No code change or release is needed. | 2026-07-30 | `grep` for `async_discover` returns exactly one caller, in `async_setup_entry`; `_async_update_data` iterates `self.nodes` only |
| WORK-05 | **Closed — not actionable from this repository, and not worth tracking.** The icon works everywhere in Home Assistant; it is grey in exactly one place, the HACS store list. HACS ships its own bundled frontend pinned to an **older** `homeassistant-frontend` submodule (`3ffbd43`) whose `brandsUrl()` builds a CDN URL directly — `https://brands.home-assistant.io/_/{domain}/dark_icon.png` — with no local-API path and no token. That URL returns the grey placeholder for `ha_somfy`. Current HA frontend instead returns `/api/brands/integration/{domain}/…`, which is why HA's own UI works. **No custom integration created after `home-assistant/brands` closed to them can have a HACS store-list icon** until HACS updates its vendored frontend, at which point this fixes itself with no action here. Cosmetic, and confined to that one list. | 2026-07-30 | Verified on the live system after installing v0.1.1: renders in *Add Integration*, absent from the HACS store list; CDN URL fetched directly and returns the placeholder |

---

## Maintenance process

1. **Review this file at the start of any work session**, and again before closing one out.
2. **Add** newly discovered items immediately, with a fresh stable ID, priority, and a concrete
   *Done when*. Never reuse an ID.
3. **Complete:** verify with real evidence first (per the `verify` discipline — fresh command,
   test, or MCP output, not assumption), then **move** the row to **Completed** with the date.
   Moving means *cutting*, not copying — a row must never exist in both places.
4. **Delete** items that are no longer relevant, and note the deletion in the revision log.
5. **Commit each revision** and push to `origin/main`.
6. Items large enough to need design work graduate to the the lifecycle toolkit lifecycle
   (`/new-requirement` → `docs/ai/<phase>/feature-<name>.md`) and link back to their ID here.

## Revision log

| Date | Change |
|------|--------|
| 2026-07-30 | **AREA-01 closed — the migration's manual half is finished.** All 24 group devices carry a room; nothing is left unassigned. Three placements differ from Homebridge and all three are the owner's deliberate corrections of pre-existing mis-assignments, which is why they were queried rather than silently "fixed" to match the old areas. What remains of the migration is MIGRATE-09, and that is mine. |
| 2026-07-30 | **The entity-ID takeover is done and verified** (MIGRATE-07/08). All 24 former Homebridge IDs now belong to HA Somfy, all 24 `*_2` twins survive as a fallback, and capability is unchanged on both sides of the swap. Verified by comparison rather than inspection: 0 missing, 0 unexpected, 0 bad states. **One consequence nobody had accounted for:** the group entities now have *no* area. Before v0.4.0 they inherited `it_equipment` from the gateway device; giving each group its own device left those devices unassigned, so they went from wrong-room to no-room. AREA-01 rewritten accordingly. Added NAME-02: display names now come from the gateway's group labels rather than the Homebridge names, which is cosmetic but visible. |
| 2026-07-30 | **Migration approach reversed, at the owner's direction.** OPEN-01 had settled on HA Somfy keeping its own `cover.somfy_uai_*` IDs with every reference rewritten. The owner wants the opposite: HA Somfy adopts the Homebridge entity IDs, Homebridge moves aside to `*_2`. Better end state, and much less work — every reference is by entity ID, so they resolve to the new integration with no edits. MIGRATE-02/03/04 replaced by MIGRATE-07/08/09; AREA-01 rewritten and promoted, since v0.4.0 (MIGRATE-06) makes each group its own device and the area now rides on the device. **Two constraints found while planning:** the order must be free-then-take, not take-then-free; and Home Assistant's "update all references" prompt must be *declined* on the `*_2` renames, or all 68 dashboards follow Homebridge instead. Also established that steps 2-4 cannot be done from this side at all — they are entity/device registry operations and there is no registry tool, so they are the owner's to click. |
| 2026-07-30 | **Irismo state verified working on hardware (IRIS-02), after two follow-up releases.** v0.3.0 shipped the feature broken *and* undiagnosable, which is the more instructive half. The bug: a stale session answers HTTP 200 with another node's payload rather than 403, so re-authentication keyed on 403 never fired and the client retried a dead session forever (IRIS-04, v0.3.1). Finding that took an hour of probing from outside the process, because every failure path logged at debug on a system with on-disk logging off — the feature could not say whether the password was wrong, the endpoint unreachable, or the whole thing simply off. That gap is now closed with a one-shot WARNING naming the last login result and read failure, plus a `web` section in diagnostics. Then the owner's reboot exposed the cost of the 49-node HTTP label sweep: `ha_somfy` became the last integration to finish setup and entities took ~5 minutes to appear, so the sweep was removed and the group-name request retried (IRIS-05, v0.3.2). **IRIS-03 deleted** — it asked whether HTTP labels resolve once a session exists, and the label path no longer exists. Added IRIS-06: the group names lost to that congested startup come back on the next reload. |
| 2026-07-30 | **IRIS-01 — Irismo motors gained open/closed state, and a Phase 1 conclusion was overturned.** §1 of the protocol findings took "telnet refuses `sdn.status.position`" to mean an Irismo has no state to report. The owner's screenshot of the UAI+ web UI showing `POSITION 1000 (100 %)` for node `40.FC.FC` disproved it. The value lives only in `somfy_device.json`, is modelled from the last command rather than measured, and is therefore right for open/closed but never intermediate — so the entities gain state and still, correctly, no slider. **Two incidental discoveries, both invisible until now:** `async_fetch_device_labels` has never worked in production, because that endpoint needs a session and the probes that "proved" it unauthenticated were running on the owner's workstation and riding its browser session; and diagnostics dumped `entry.options` verbatim, which was safe only until a credential was stored there. Added IRIS-02/03. The `somfy-sdn` skill was corrected on both counts — it had recorded the endpoint as unauthenticated and Irismo as stateless. |
| 2026-07-30 | **All four optional items closed**, at the owner's request. CI: **179 tests**, up from 140. CAP-02 was built test-first — the red step is commit `67152f2`, tests only, which failed CI on three import errors before any implementation existed. TEST-01 explicitly was *not*: the behaviour already existed and was correct, so its tests were green on arrival, which is the whole reason the gap was worth filling. Two things surfaced while building CAP-02 that were not in its description: the options flow replaced the options dict wholesale, so setting an override would have silently reset the poll interval; and an override applied at the entity would have left a forced-positional motor unpolled, so it belongs at the coordinator. WORK-04 resolved toward amending `CLAUDE.md` rather than adding a hook — auto-push cannot make the risk judgment the same section requires. **Only the migration workstream remains open.** |
| 2026-07-30 | **All three parked items closed**, at the owner's decision: each waits on an outside party — the owner for HW-01 and MIGRATE-05, HACS for WORK-05 — and in every case the fix arrives without work in this repository, so tracking them only clogged the list. Closed rather than deleted, because each carries a finding worth keeping: `false` position means faulty hardware (HW-01), the HACS store-list icon is unreachable by design for every post-2026.3 custom integration (WORK-05). **One correction made while closing:** the premise that recovery is automatic holds for HW-01 — a repaired motor is already `POSITIONAL` from its type string and polled every cycle, so its position simply reappears — but *not* for MIGRATE-05. `async_discover` runs once at setup and the poll cycle never re-reads group membership, so re-adding `077537` to `RoomB B/O` needs an integration reload. That caveat now lives in the MIGRATE-05 evidence, where it will be found when the motor is repaired. **No parked items remain.** |
| 2026-07-30 | **Full reconciliation — the file had drifted badly.** 20 of the 36 rows above the fold were *also* in Completed: every item closed since Phase 2 had been copied down rather than moved, so the backlog advertised 36 open items when 16 were open and, after inspection, only 11 genuinely are. Fixed by re-deriving status from the repository rather than from the rows. Corrections in the other direction: **GROUP-02** was implemented all along (`cover.py:206-211`) and **HACS-03** shipped six releases ago; both moved to Completed. **HW-03**, **NAME-01** and **OPEN-01** carried status `done` but had never been moved. **CAP-03** appeared twice *inside* Completed and is now one row carrying both its CI and live evidence. **HACS-02** claimed 123 tests; CI actually reports 140. **WORK-04** understated itself — the `post-commit` hook is not merely untracked, it does not exist, so every push so far has been manual. **WORK-01** was never a task and has been promoted to a standing constraint at the top, where it cannot be completed away. Phase-shaped section headings retired, since all five had emptied; what is left is grouped by what it actually is — the migration workstream, and everything deferred. New: **TEST-01**, no test covers `extra_state_attributes` on either entity class. |
| 2026-07-30 | **Parked section removed**, at the owner's decision. PARK-01 was contributing fixes upstream to `peter-dolkens/somfy-uai`, dropped because that repo has one commit, no license, and two unanswered issues — there is nothing to contribute to. PARK-02 was driving raw SDN over RS-485 without the gateway, dropped because the UAI+ works and performs the framing; the protocol details remain in the design doc should it ever be wanted. Neither blocked anything. |
| 2026-07-30 | **WORK-02 deleted** (rotate the gateway password), at the owner's decision. Both credentials are confirmed absent from git and live only in the gitignored `scripts/secrets.local.json` and the HA config entry, which is where a config-entry password belongs. The device is LAN-only, so the threat model requires an attacker already inside the network and the session store. Rotating would have broken the probe scripts and the HA entry for no meaningful gain. **The standing rule is unchanged: credentials are never committed.** |
| 2026-07-29 | **PERF-03, HW-04, PROTO-07 and PROTO-09 all closed.** PERF-03 verified live by the owner. HW-04 added as a deliberately narrow check — it warns only when a name matches *another* existing group, so the current fleet produces zero warnings while the real `136E33` case would still fire. PROTO-09: the `sdn.` prefix is optional, both forms return identical payloads. PROTO-07: closed as not worth pursuing, since `somfy_devices.json` is 403 even with basic auth and the per-node endpoint returns more anyway. Gateway device now carries firmware and serial from `about.json`. |
| 2026-07-29 | **Brand icon mechanism corrected.** The `custom_components/ha_somfy/brand/` folder was already the right answer: since HA 2026.3 custom integrations serve their own brand images locally and `home-assistant/brands` no longer accepts them (PR #10871 closed on that basis). The grey placeholder in the HACS store list is the CDN fallback for a not-yet-installed integration, not a defect. Added WORK-05/06; released v0.1.1 so the install picks up the redrawn icon. |
| 2026-07-29 | **`false` position explained.** Owner confirmed `077537` "RoomB B/O 3" is physically disconnected pending repair and was deliberately removed from its group. That makes both `false`-replying motors known-faulty hardware, 2 for 2 against 38 healthy motors returning numbers — so `{"result": false}` signals a motor-side fault, not the "unset limits" originally guessed. Q2 in the protocol findings is resolved; MIGRATE-05 downgraded to parked (no action needed). |
| 2026-07-29 | **OPEN-01 resolved.** New entities keep their own IDs; references migrate to them. Collision check showed no `_2` suffixes will occur, so nothing needs renaming. Added MIGRATE-01..05 and `entity-migration-map.md` with the full 24-row mapping. Two findings from building it: the four all-Irismo groups currently advertise a position capability they have never had, and `077537` belongs to no group so `RoomB B/O` reaches only 2 of 3 motors. |
| 2026-07-29 | **Correction:** the 24 `cover.*` entities are Homebridge-bridged and live, not orphans. WORK-01 rewritten from "purge them" to "preserve them". Added OPEN-01: freeing the old entity IDs does not by itself make the new group entities adopt them, since IDs come from the gateway's group names. Phase 6 is on hold at the owner's request. |
| 2026-07-29 | **Phases 2-5 complete, CI fully green** (123 tests, hassfest, HACS 9/9, ruff). Two failures only CI could catch: pytest-socket (pulled in by the HA harness) blocked the client tests' loopback socket, and HACS required repository topics plus brand assets. Remaining before release: PROTO-08 position polarity and WORK-01 orphan purge, both needing live hardware. |
| 2026-07-29 | **Phase 1 gate passed.** PROTO-01..06 completed. Added PROTO-08/09, CAP-04/05/06, GROUP-03, HW-01/02/03, PERF-04. Key correction: the classifier must match **`SDN Module`**, not `irismo` — the planned substring list would have matched none of the 9 Irismo. Second correction: the bus is fast, not slow, so the cautious polling assumption in the plan was unnecessary. |
| 2026-07-29 | Created. Seeded with 30 items from the project plan and the live gateway reconnaissance (ports open, 25 group names read, `somfy_devices.json` 403, 1811129 confirmed on all 9 Irismo). |
