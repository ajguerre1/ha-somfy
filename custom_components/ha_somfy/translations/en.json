{
  "config": {
    "step": {
      "user": {
        "title": "Connect to the Somfy UAI+",
        "description": "Enter the address and telnet credentials of your Somfy Connect UAI+ gateway. Motors and groups are discovered automatically.",
        "data": {
          "host": "Host",
          "port": "Port",
          "username": "Username",
          "password": "Password"
        }
      },
      "confirm": {
        "title": "Confirm discovered devices",
        "description": "Found {motors} motor(s) on {host}: {positional} report position, {non_positional} are open/close/stop only (for example Irismo motors behind an SDN bridge). {groups} group(s) detected.\n\nMotor entities are created but disabled by default so they do not add state updates until you need them. Group entities are enabled."
      }
    },
    "error": {
      "cannot_connect": "Could not reach the gateway. Check the host and port.",
      "invalid_auth": "The gateway rejected those credentials.",
      "no_motors": "Connected, but the gateway reported no motors.",
      "unknown": "Unexpected error. Check the Home Assistant log."
    },
    "abort": {
      "already_configured": "This gateway is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HA Somfy options",
        "menu_options": {
          "polling": "Polling",
          "capability": "Motor capability"
        }
      },
      "polling": {
        "title": "Polling",
        "description": "How often to poll motors that report position. Motors without position feedback are never polled.",
        "data": {
          "poll_interval": "Poll interval"
        }
      },
      "capability": {
        "title": "Motor capability",
        "description": "Correct a motor whose capability was detected wrongly.\n\nCapability is normally read from the motor type the gateway reports, so this is only needed for hardware this integration does not recognise. Changing it reloads the integration.",
        "data": {
          "motor": "Motor"
        }
      },
      "motor": {
        "title": "Capability",
        "description": "{motor}\n\nAutomatic uses the type the gateway reports. Reports position adds the position slider and starts polling this motor. No position feedback removes the slider and stops polling it.",
        "data": {
          "capability": "Capability"
        }
      }
    },
    "abort": {
      "no_motors": "No motors are loaded for this gateway yet. Try again once the integration has finished starting."
    }
  },
  "selector": {
    "capability": {
      "options": {
        "auto": "Automatic (use the detected motor type)",
        "positional": "Reports position",
        "non_positional": "No position feedback"
      }
    }
  }
}
